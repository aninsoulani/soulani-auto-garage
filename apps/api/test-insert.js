const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function create() { const v = await prisma.vehicle.create({ data: { slug: 'test-suv', make: 'Toyota', model: 'Fortuner', year: 2023, carType: 'SUV', color: 'White', listingType: 'SALE', status: 'AVAILABLE', description: 'Test SUV', salesListing: { create: { price: 500000000, previousOwners: 0 } } } }); console.log(v); } create().catch(console.error).finally(() => prisma.$disconnect());
